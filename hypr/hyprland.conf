
# #######################################################################################
# AUTOGENERATED HYPRLAND CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hyprland.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################

autogenerated = 0 # remove this line to remove the warning

# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf





################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=HDMI-A-1,1920x1080@143,98x0,1



###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/



#################
### AUTOSTART ###
#################


env = MOZ_ENABLE_WAYLAND,1
env = ELECTRON_OZONE_PLATFORM_HINT,wayland
env = XDG_CURRENT_DESKTOP,Hyprland
env = SDL_VIDEODRIVER,wayland
env = QT_QPA_PLATFORM,wayland
env = XDG_SESSION_TYPE, wayland
env = XDG_SESSION_DESKTOP, Hyprland
env = MOZ_DBUS_REMOTE,1
env = LIBVA_DRIVER_NAME,nvidia
env = __GLX_VENDOR_LIBRARY_NAME,nvidia
env = NVD_BACKEND,direct
env = CLUTTER_BACKEND,wayland
env = GBM_BACKEND,nvidia-drm
env = _JAVA_AWT_WM_NONREPARETING,1

exec-once = dbus-update-activation-environment  DISPLAY WAYLAND_DISPLAY XDG_CURRENT_DESKTOP XDG_SESSION_TYPE
#start xdg sessions for wayland
exec-once = gentoo-pipewire-launcher
exec-once = ~/.config/hypr/start &
exec-once = waybar &
exec-once = foot --server &
exec-once = flameshot &
exec-once = swaybg -i ~/pictures/storm.jpg -m fill 



# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

# exec-once = $terminal
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox


#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/
env=XCURSOR_THEME,Adwaita
env = XCURSOR_SIZE,16


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general {
    gaps_in = 10
    gaps_out = 10

    border_size = 2

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
	col.active_border = rgba(7fb4caff)
    col.inactive_border = rgba(0d0c0cff)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = false

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = master
}

ecosystem {
	no_update_news = true
	no_donation_nag = true

}


gesture = 3, horizontal, workspace

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 10

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    shadow {
        enabled = false
        range = 4
        render_power = 3
        color = rgba(1a1a1aee)
    }

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = false
        size = 3
        passes = 1

        vibrancy = 0.1696
    }
}

cursor {
	zoom_factor = 0.5
}

# xwayland { 
#     enabled = true
#     create_abstract_socket = true
# }


# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = yes, please :)

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = easeOutQuint,0.23,1,0.32,1
    bezier = easeInOutCubic,0.65,0.05,0.36,1
    bezier = linear,0,0,1,1
    bezier = almostLinear,0.5,0.5,0.75,1.0
    bezier = quick,0.15,0,0.1,1

    animation = global, 1, 10, default
    animation = border, 1, 5.39, easeOutQuint
    animation = windows, 1, 4.79, easeOutQuint
    animation = windowsIn, 1, 4.1, easeOutQuint, popin 87%
    animation = windowsOut, 1, 1.49, linear, popin 87%
    animation = fadeIn, 1, 1.73, almostLinear
    animation = fadeOut, 1, 1.46, almostLinear
    animation = fade, 1, 3.03, quick
    animation = layers, 1, 3.81, easeOutQuint
    animation = layersIn, 1, 4, easeOutQuint, fade
    animation = layersOut, 1, 1.5, linear, fade
    animation = fadeLayersIn, 1, 1.79, almostLinear
    animation = fadeLayersOut, 1, 1.39, almostLinear
    animation = workspaces, 1, 1.94, almostLinear, fade
    animation = workspacesIn, 1, 1.21, almostLinear, fade
    animation = workspacesOut, 1, 1.94, almostLinear, fade
}

# Ref https://wiki.hyprland.org/Configuring/Workspace-Rules/
# "Smart gaps" / "No gaps when only"
# uncomment all if you wish to use that.
# workspace = w[tv1], gapsout:0, gapsin:0
# workspace = f[1], gapsout:0, gapsin:0
# windowrulev2 = bordersize 0, floating:0, onworkspace:w[tv1]
# windowrulev2 = rounding 0, floating:0, onworkspace:w[tv1]
# windowrulev2 = bordersize 0, floating:0, onworkspace:f[1]
# windowrulev2 = rounding 0, floating:0, onworkspace:f[1]

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_status = master
	inherit_fullscreen=true
	new_on_top = true
	smart_resizing = true
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc {
	force_default_wallpaper = 0
	focus_on_activate = true
    disable_hyprland_logo = true # If true disables the random hyprland logo / anime girl background. :(
	font_family = "JetBrains Mono:style=Regular:pixelsize:11:antialias=true:autohint=true"
	vfr = true
}





#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us,ru
    kb_variant =
    kb_model =
    kb_options = grp:caps_toggle
    kb_rules =

    follow_mouse = 1
	repeat_delay = 200
    repeat_rate = 50
	accel_profile = flat


    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = false
		disable_while_typing = false
    }
}


# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


cursor {
    enable_hyprcursor = false
}






###########################
### DWL|DWM-STYLE KEYBINDS  ###
###########################




$mod = ALT
$shift = SHIFT
$ctrl = CTRL
$menu = mew-run
$term = footclient

bind = $mod, RETURN, swapnext

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more

# run apps
bind = $mod, p, exec, $menu
bind = $mod SHIFT, RETURN, exec, $term
bind = , PRINT, exec, flameshot gui


#close and quit
bind = $mod SHIFT, C, killactive
bind = $mod SHIFT, Q, exit


# float / fullscreen
bind = $mod, e, fullscreen




#focus
bind = $mod, j, movefocus, d
bind = $mod, k, movefocus, u
bind = $mod, h, movefocus, l
bind = $mod, l, movefocus, r






#move stack
bind = $mod, left, movefocus, l
bind = $mod, right, movefocus, r
bind = $mod, up, movefocus, u
bind = $mod, down, movefocus, d

#monocle type shit
bind = $mod SHIFT, j, layoutmsg, cycleprev
bind = $mod SHIFT, k, layoutmsg, cyclenext

#resize
binde = $mod SHIFT, h, resizeactive, -60 0
binde = $mod SHIFT, l, resizeactive, 60 0

# layouts
bind = $mod, f, togglefloating




# switch workspaces
bind = $mod, 1, workspace, 1
bind = $mod, 2, workspace, 2
bind = $mod, 3, workspace, 3
bind = $mod, 4, workspace, 4
bind = $mod, 5, workspace, 5
bind = $mod, 6, workspace, 6
bind = $mod, 7, workspace, 7
bind = $mod, 8, workspace, 8
bind = $mod, 9, workspace, 9

# swtich beetwen workspaces
bind = $mod SHIFT, 1, movetoworkspace, 1
bind = $mod SHIFT, 2, movetoworkspace, 2
bind = $mod SHIFT, 3, movetoworkspace, 3
bind = $mod SHIFT, 4, movetoworkspace, 4
bind = $mod SHIFT, 5, movetoworkspace, 5
bind = $mod SHIFT, 6, movetoworkspace, 6
bind = $mod SHIFT, 7, movetoworkspace, 7
bind = $mod SHIFT, 8, movetoworkspace, 8
bind = $mod SHIFT, 9, movetoworkspace, 9



#switch monitors
bind = $mod, comma, focusmonitor, l
bind = $mod, period, focusmonitor, r
bind = $mod SHIFT, comma, movewindow, mon:l
bind = $mod SHIFT, period, movewindow, mon:r

bind = $mod ,D,  layoutmsg, swapwithmaster













bind = $mod, TAB, exec, hyprctl dispatch workspace prev




# Example special workspace (scratchpad)
bind = $mod, S, togglespecialworkspace, magic
bind = $mod SHIFT, S, movetoworkspace, special:magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mod, mouse_down, workspace, e+1
bind = $mod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging

bindm = $mod, mouse:272, movewindow
bindm = $mod, mouse:273, resizewindow
##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$

# Ignore maximize requests from apps. You'll probably like this.
windowrulev2 = suppressevent maximize, class:.*

# Fix some dragging issues with XWayland
windowrulev2 = nofocus,class:^$,title:^$,xwayland:1,floating:1,fullscreen:0,pinned:0A


windowrule = suppressevent fullscreen, class:flameshot, title:flameshot
windowrule = rounding 0, class:flameshot, title:flameshot
windowrule = noborder, class:flameshot, title:flameshot
windowrule = float, class:flameshot, title:flameshot
windowrule = fullscreenstate, class:flameshot, title:flameshot
windowrule = move 0 0, class:flameshot, title:flameshot
windowrule = stayfocused, class:flameshot, title:flameshot
windowrule = size 1920 1080, class:flameshot, title:flameshot
windowrule = minsize 1920 1080, class:flameshot, title:flameshot
windowrule = maxsize 1920 1080, class:flameshot, title:flameshot
windowrule = pin, class:flameshot, title:flameshot
windowrule = float, class:flameshot, title:Upload image
windowrule = float, class:flameshot, title:Configuration
windowrule = float, class:flameshot, title:Capture Launcher
windowrule = float, class:flameshot, title:Save screenshot

#float
windowrule = float, title:^(Picture-in-Picture)$
windowrule = float, title:^(Preview)$
windowrule = float, title:^(Save As)$



#pin
windowrule = pin, title:^(Picture-in-Picture)$



